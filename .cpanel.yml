deployment:
      tasks:
        - export DEPLOYPATH=/home/migra197/public_html/admin/winter2024-demo  # Add /<app_folder> if required
        - /bin cp * $DEPLOYPATH                           #copy all from root 
